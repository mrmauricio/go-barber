// history é uma lib que possibilita navegar o usuário para rotas de todos
// os lugares, incluindo do próprio redux
import { createBrowserHistory } from 'history';

const history = createBrowserHistory();

export default history;
